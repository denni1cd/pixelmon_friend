# Tactical Plan

1. Extend shared state and helpers for wood stock inspection and maintenance-mode state.
2. Add deterministic commands/capabilities for stock check, one-shot maintenance, and maintenance mode enable/disable.
3. Add optional idle maintenance scheduling in the app runtime.
4. Add regression coverage for parser, intent normalization, status, and maintenance workflow behavior.
