# Task 1 Verification

## Commands Run
- `npm test`
- `node -e "const { createCapabilities } = require('./src/capabilities'); console.log(createCapabilities().map((cap) => cap.name).join(','));"`

## Result
- Passed.
- New Phase 1 capabilities are present in the registry load output.
