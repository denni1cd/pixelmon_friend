# Task 1 Notes

## Scope
- Runtime core contracts.
- Capability registry.
- Task manager lifecycle.

## Changes
- Added `src/runtime/task-manager.js`.
- Added `src/runtime/capability-registry.js`.
- Added `src/runtime/command-router.js`.
- Added `src/runtime/create-runtime.js`.
- Added shared config and helper services under `src/`.

## Outcome
- The bot now has a reusable modular shell instead of a monolithic chat handler.
