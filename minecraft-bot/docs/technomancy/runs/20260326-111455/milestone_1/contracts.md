# Contracts

- Tool preference remains centralized in shared helper/resource contracts.
- New tool commands are additive only.
- Milestone 2 may assume `equip axe` and `equip pickaxe` exist and that tool crafting returns `{ crafted, total }`.
