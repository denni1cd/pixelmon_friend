# Task 1 Verification

- Command run: `npm test`
- Result: pass (`37/37`)
- Verified:
  - parser routes for `cobble` and `stone tools`
  - cobble loop capability composition
  - stone-tools capability behavior
