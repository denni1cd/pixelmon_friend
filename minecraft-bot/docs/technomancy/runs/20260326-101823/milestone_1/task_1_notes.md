# Task 1 Notes

- Added `src/bot/state.js` and attached runtime state to `context.state`.
- Extended the command parser for home/chest save/query commands plus `wood run`.
- Added capability modules for `set home`, `set chest`, `where is home`, `where is chest`, and `go home`.
- Registered the new capabilities in the capability index.
