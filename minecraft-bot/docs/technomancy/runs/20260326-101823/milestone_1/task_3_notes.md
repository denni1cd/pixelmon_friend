# Task 3 Notes

- Added `wood-run` capability built on registry-resolved gather and deposit capability contracts.
- Exposed reusable `perform` functions on `gather-wood` and `deposit` so composed workflows do not duplicate logic or violate the single-task rule.
- Added workflow tests and finalized run artifacts.
