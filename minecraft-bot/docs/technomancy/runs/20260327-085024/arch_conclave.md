# Arch Conclave

- Authorized `milestone-1` as the contract-first foundation.
- Locked the approved migration strategy to additive adapters over the existing helper/capability substrate.
- Recorded a mid-run convergence decision: use the existing in-branch `tool-runtime` implementation as the base and surface it through a stricter factory API instead of carrying two runtimes.
- Authorized `milestone-2` after the runtime factory exposed normalized execution, legacy command mapping, and tool history.
- Authorized `milestone-3` after the objective runner and recovery policy were wired into the same runtime surface.
- Final integration status: complete, with tests passing.
