# Milestone 3 Contracts

- MCP calls use the same registry and policy gate as internal callers.
- Chat/runtime rewiring must not remove capability fallbacks.
- The MCP server remains an adapter over the internal runtime, not an independent execution path.
